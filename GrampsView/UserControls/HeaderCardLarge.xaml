<?xml version="1.0" encoding="UTF-8" ?>
<Grid
    x:Class="GrampsView.UserControls.HeaderCardLarge"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:models="clr-namespace:GrampsView.Data.Model"
    xmlns:ucon="clr-namespace:GrampsView.UserControls"
    x:Name="HeaderCardLargeRoot"
    Padding="0"
    x:DataType="models:HLinkHeaderModel"
    BindingContextChanged="HeaderCardLargeRoot_BindingContextChanged"
    Style="{DynamicResource GridLargeVariableCardStyle}">

    <Frame
        x:Name="headerFrame"
        BorderColor="Transparent"
        Style="{DynamicResource FrameLargeVariableCardStyleNS}">

        <ucon:ListLineCardLarge x:Name="ListLine" BindingContext="{DynamicResource HeaderCard}" />
    </Frame>
</Grid>