<ucon:SmallCardControlTemplate
    x:Class="GrampsView.UserControls.NoteCardSmall"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:common="clr-namespace:GrampsView.Common"
    xmlns:models="clr-namespace:GrampsView.Data.Model"
    xmlns:sharesharp="clr-namespace:SharedSharp.CommonRoutines"
    xmlns:ucon="clr-namespace:GrampsView.UserControls"
    x:DataType="models:HLinkNoteModel">

    <Grid>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*" />
            <ColumnDefinition Width="Auto" />
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Label
            Grid.Row="0"
            Grid.Column="0"
            Style="{DynamicResource GVCardLabelStyleSmall}"
            Text="{Binding DeRef, Mode=OneWay}" />

        <Label
            Grid.Row="0"
            Grid.Column="1"
            FontSize="{x:Static sharesharp:FontSizes.FontVerySmall}"
            FormattedText="{Binding DeRef.GType, Mode=OneWay}"
            HorizontalOptions="End"
            Style="{DynamicResource GVCardLabelStyleSmall}" />
    </Grid>
</ucon:SmallCardControlTemplate>