<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="GrampsView.Views.SearchPage"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:behaviour="clr-namespace:GrampsView.Behaviours"
    xmlns:converters="clr-namespace:GrampsView.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:i18n="clr-namespace:GrampsView.Assets.Strings"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ucon="clr-namespace:GrampsView.UserControls"
    xmlns:viewmodels="clr-namespace:GrampsView.ViewModels"
    x:Name="SearchPageRoot"
    x:DataType="viewmodels:SearchPageViewModel"
    Appearing="ContentPage_Appearing"
    ControlTemplate="{StaticResource DetailPageTemplate}"
    Style="{DynamicResource GVBaseContentPage}"
    mc:Ignorable="d">

    <!--  Header  -->
    <NavigationPage.TitleView>
        <ucon:TopMenu />
    </NavigationPage.TitleView>

    <Page.Resources>
        <ResourceDictionary>

            <converters:FlipBoolConverter x:Key="flipBool" />
        </ResourceDictionary>
    </Page.Resources>

    <!--  Content  -->
    <ScrollView>
        <StackLayout>

            <SearchBar
                x:Name="SearchBar"
                Grid.Row="0"
                Margin="3"
                BackgroundColor="{AppThemeBinding Dark={StaticResource SurfaceColorDark},
                                                  Light={StaticResource SurfaceColorLight}}"
                HorizontalOptions="FillAndExpand"
                HorizontalTextAlignment="Start"
                IsTabStop="True"
                SearchCommand="{Binding SearchCommand}"
                SearchCommandParameter="{Binding Text, Source={x:Reference SearchBar}}"
                TextColor="{DynamicResource OnSurfaceColor}">
                <SearchBar.Behaviors>
                    <behaviour:EventToCommandBehavior
                        Command="{Binding SearchButtonCommand}"
                        CommandParameter="{Binding Text, Source={x:Reference SearchBar}}"
                        EventName="SearchButtonPressed" />
                </SearchBar.Behaviors>
            </SearchBar>

            <ucon:InstructionCardLarge BindingContext="{x:Static i18n:AppResources.SearchPage_Instructions}" />

            <ucon:CardGroupBaseHeader BindingContext="{Binding PersonList, Mode=OneWay}" FsctTemplate="{StaticResource PersonCardSmallDT}" />

            <ucon:CardGroupBaseHeader BindingContext="{Binding PersonNameList, Mode=OneWay}" FsctTemplate="{StaticResource PersonNameCardSmallDT}" />

            <ucon:CardGroupBaseHeader BindingContext="{Binding FamilyList, Mode=OneWay}" FsctTemplate="{StaticResource FamilyCardSmallDT}" />

            <ucon:CardGroupBaseHeader BindingContext="{Binding EventList, Mode=OneWay}" FsctTemplate="{StaticResource EventCardSmallDT}" />

            <ucon:CardGroupBaseHeader BindingContext="{Binding NoteList, Mode=OneWay}" FsctTemplate="{StaticResource NoteCardSmallDT}" />

            <ucon:CardGroupBaseHeader BindingContext="{Binding AddressList, Mode=OneWay}" FsctTemplate="{StaticResource AddressCardSmallDT}" />

            <ucon:CardGroupBaseHeader BindingContext="{Binding CitationList, Mode=OneWay}" FsctTemplate="{StaticResource CitationCardSmallDT}" />

            <ucon:CardGroupBaseHeader BindingContext="{Binding MediaList, Mode=OneWay}" FsctTemplate="{StaticResource MediaCardSmallDT}" />

            <ucon:CardGroupBaseHeader BindingContext="{Binding PlaceList, Mode=OneWay}" FsctTemplate="{StaticResource PlaceCardSmallDT}" />

            <ucon:InstructionCardLarge BindingContext="{x:Static i18n:AppResources.SearchPage_NothingFound}" IsVisible="{Binding BindingContext.SearchItemsFound, Converter={StaticResource flipBool}, Source={x:Reference SearchPageRoot}, Mode=OneWay}" />
        </StackLayout>
    </ScrollView>
</ContentPage>