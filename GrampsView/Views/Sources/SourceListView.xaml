<?xml version="1.0" encoding="utf-8" ?>
<views:ViewBasePage
    x:Class="GrampsView.Views.SourceListPage"
     xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:ucon="clr-namespace:GrampsView.UserControls"
    xmlns:viewmodels="clr-namespace:GrampsView.ViewModels"
    xmlns:views="clr-namespace:GrampsView.Views"
    Title="{Binding BaseTitle}"
    x:DataType="viewmodels:SourceListViewModel"
    Style="{DynamicResource GVBaseContentPage}">

    <Shell.TitleView>
        <ucon:TopMenu />
    </Shell.TitleView>

    <!--  Content  -->

    <ucon:CollectionSingleCardGrouped
        FsctSource="{Binding SourceSource, Mode=OneWay}"
        FsctTemplate="{StaticResource SourceCardSmallDT}" />
</views:ViewBasePage>